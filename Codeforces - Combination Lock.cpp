#include <stdio.h>
#include <bits/stdc++.h>
using namespace std;
int main(){
     string a, b;
     int n,i,j;
     cin >> n;
     cin >> a>>b;
     for(i = 0; i < n; i++){
        int k = a[i] - b[i] + 0;
        k = abs(k);

     }
}
