#include <bits/stdc.h
using namespace std;
int main()
{
    int a, b, c;
    cin>>a>>b>>c;
    int x = max(a,b);
    int y = max(x,c);
    int m = min(a,b);
    int n = min(m,c);
    int ans = (a+b+c)-(m+n);
    int out = abs(abs())
}
